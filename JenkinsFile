// Declarative //
pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				bat 'cd monappli & mvn install'
			}
		 post {
                success {
                    junit 'Formation/monappli-service/target/surefire-reports/*.xml'
                        }
                 }
               
		}
	}
}